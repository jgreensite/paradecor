const makerjs = require('makerjs'); const { generateCncLayout } = require('../src/backplane.ts'); console.time('generate'); const rybProfiles = Array(100).fill({ width: 50, height: 200, shape: 'rectangular' }); const rybPositions = Array(100).fill(0).map((_, i) => ({ x: i * 20, y: Math.sin(i) * 50 })); const backplaneParams = { enabled: true, shape: 'organic', organicOffset: 20, materialThickness: 12, slotDepth: 100, dogboneRadius: 4 }; console.log('Starting...'); const layout = generateCncLayout(rybProfiles, backplaneParams, rybPositions); console.timeEnd('generate');
